<UserControl
  x:Class="tweetz.core.Controls.UserProfileBlock.UserProfileFollowingBlock"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:local="clr-namespace:tweetz.core.Controls.UserProfileBlock"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:services="clr-namespace:tweetz.core.Services"
  d:DesignHeight="450"
  d:DesignWidth="800"
  mc:Ignorable="d">

  <UserControl.Resources>
    <Style
      x:Key="FollowingStyle"
      BasedOn="{StaticResource BaseTextBlockStyle}"
      TargetType="TextBlock">
      <Setter Property="FontWeight" Value="Bold" />

      <Style.Triggers>
        <DataTrigger
          Binding="{Binding IsFollowing, Mode=OneWay}"
          Value="True">
          <Setter Property="Text" Value="{services:LanguageService profile-following}" />
        </DataTrigger>
        <DataTrigger
          Binding="{Binding IsFollowing, Mode=OneWay}"
          Value="False">
          <Setter Property="Text" Value="{services:LanguageService profile-not-following}" />
        </DataTrigger>
      </Style.Triggers>
    </Style>
  </UserControl.Resources>

  <TextBlock Style="{StaticResource FollowingStyle}" />
</UserControl>