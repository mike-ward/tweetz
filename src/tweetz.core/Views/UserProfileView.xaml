<UserControl x:Class="tweetz.core.Views.UserProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:userProfileControls="clr-namespace:tweetz.core.Views.UserProfileControls"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <userProfileControls:UserProfileBackBar Grid.Row="0" />
    <userProfileControls:UserProfileBanner Grid.Row="1" />
    <userProfileControls:UserProfileInfo Grid.Row="2" Margin="15 3" />
    <Separator Grid.Row="3" Margin="15 10" />
    <userProfileControls:UserProfileTimeline Grid.Row="4" Margin="5 0" />
  </Grid>
</UserControl>